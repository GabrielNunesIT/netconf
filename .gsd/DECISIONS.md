# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | Project setup | architecture | Go module path | github.com/GabrielNunesIT/netconf | User-specified module path for the project. | No | agent |
| D002 | Project setup | architecture | Server library interaction model | Handler callbacks — implementers register per-operation handlers, library owns session lifecycle and framing dispatch | Simpler than middleware chains, clear separation between protocol machinery and business logic. Most natural model for NETCONF where operations are well-defined. | No | agent |
| D003 | Project setup | architecture | Go package layout | netconf (top-level shared types), netconf/transport/ssh, netconf/transport/tls, netconf/server, cmd/netconf | Flat with transport subpackages. Clean transport separation, follows nemith/netconf pattern. Transport interface decouples framing from protocol logic. | Yes | agent |
| D004 | Project setup | architecture | NETCONF framing mode support | Both base:1.0 (end-of-message ]]>]]>) and base:1.1 (chunked framing) with auto-negotiation during hello exchange | Older devices only speak 1.0. Supporting both ensures interop across the full device landscape. Auto-negotiation based on base capability in hello is per-spec. | No | agent |
| D005 | Project setup | architecture | Concurrency model | Sequential RPC processing per session on server; client supports concurrent RPCs with message-id matching | Sequential server-side is simpler, correct, covers 95% of use cases. Concurrent pipelining can be added as a capability later. Client concurrency is needed for practical use. | Yes | agent |
| D006 | Project setup | architecture | Error handling strictness | Strict — reject non-conformant messages with proper rpc-error responses | This is a reference-quality library for open-source community. Strict compliance ensures correctness and helps identify non-conformant peers early. | Yes | agent |
| D007 | Project setup | library | Approved dependency set | Go stdlib + golang.org/x/* + github.com/stretchr/testify + github.com/vektra/mockery. All others require explicit approval. | Minimal dependency surface for an open-source library. testify and mockery are battle-tested Go testing tools that complement loopback transport testing. | Yes | agent |
| D008 | Project setup | architecture | Test transport strategy | In-process loopback via io.Pipe. Client↔server connected in-memory. No real SSH daemon needed for tests. | Fast, isolated, no infrastructure. testify for assertions, mockery for handler mocks. Real SSH only needed for transport-specific conformance (deferred). | Yes | agent |
| D009 | Project setup | architecture | YANG support scope | RFC-defined YANG modules only (ietf-netconf-monitoring, ietf-netconf, ietf-netconf-acm, ietf-yang-library, ietf-netconf-nmda) as hand-coded Go structs. No general YANG parser. | Covers what the protocol itself needs to function. A general YANG parser is a separate project. Hand-coded structs are simpler, faster, and have no runtime parsing overhead. | Yes | agent |
| D010 | M001/S01 planning | architecture | Session construction — separate client/server constructors | Two constructor functions: ClientSession(transport, localCaps) and ServerSession(transport, localCaps, sessionID). Server assigns session-id, client receives it. Both perform hello exchange and framing negotiation. | Client and server have asymmetric hello behavior: server includes session-id, client does not. Separate constructors make this explicit. A single Session struct is shared — only construction differs. | Yes | agent |
| D011 | M001/S01/T01 | architecture | Capability type as string alias | `Capability = string` (type alias, not new type). CapabilitySet is `[]Capability`. | Plain strings interop seamlessly with encoding/xml and the Hello struct's `[]string` capabilities field. A distinct type would require conversion at the XML boundary with no safety benefit since capability URN correctness is validated by ValidateURN, not the type system. | Yes | agent |
| D012 | M001/S01/T01 | architecture | Transport.Upgrader as a separate optional interface | Upgrade() is on a separate `Upgrader` interface, not embedded in `Transport`. Session code type-asserts to Upgrader if available. | Not all transports need to support upgrade (e.g. a read-only replay transport). Keeping it separate avoids forcing every Transport implementor to provide a no-op Upgrade method. | Yes | agent |
| D013 | M001/S01/T02 | architecture | Framer API: Upgrade() panics rather than returns an error on double-call | Upgrade() panics with a descriptive message on second call | Double-Upgrade is a programming error, not a runtime condition. Panicking surfaces the bug immediately at the call site with a clear message. An error return would require every caller to check it and handle an impossible-to-recover condition. The panic is covered by TestUpgrade_PanicsIfCalledTwice and is visible in go test output. | Yes | agent |
| D014 | M001/S01/T04 | testing | SSH transport test connectivity method | Loopback TCP (net.Listen("tcp", "127.0.0.1:0") + Dial) instead of net.Pipe() | The SSH handshake starts by writing the version banner before reading. net.Pipe() is unbuffered — both sides writing before either reads causes deadlock. The x/crypto/ssh test suite documents this same issue and uses the same workaround. Loopback TCP has kernel buffers that absorb the initial writes. This is the only transport-level test using real TCP; all session/framer tests remain on io.Pipe. | Yes | agent |
| D015 | M001/S02/T01 | architecture | RPCError.Info field type | []byte with xml:",innerxml" | <error-info> has no fixed schema; it can contain arbitrary XML children. Using []byte with innerxml preserves the raw sub-elements without requiring a schema. Callers who need structured access decode the bytes independently. Same pattern as RPC.Body and RPCReply.Body. | Yes | agent |
| D016 | M001/S02/T01 | architecture | ParseRPCErrors synthetic root strategy | Wrap RPCReply.Body in <wrapper>…</wrapper> before xml.Unmarshal | encoding/xml requires exactly one root element. RPCReply.Body may contain multiple sibling <rpc-error> elements with no enclosing root. Wrapping in a synthetic element is the only reliable way to decode them into a slice without a custom scanner. | No | agent |
| D017 | M001/S02/T01 | architecture | ParseRPCErrors return semantics (three states) | (nil, nil) = ok/empty reply; ([]RPCError, nil) = errors decoded; (nil, err) = malformed XML | Three distinct states cover all caller needs without ambiguity. Callers must not treat (nil, nil) as a parse failure — it signals a successful <ok/> reply. Error state (nil, err) is only for genuinely unparseable body bytes. | No | agent |
| D018 | M001/S02/T02 | architecture | Datastore boolean fields as *struct{} | Running, Candidate, Startup are *struct{} with omitempty | Gives "element present or absent" child-element encoding without a custom marshaler. &struct{}{} emits the element; nil omits it. Idiomatic Go+encoding/xml pattern. URL stays string with omitempty for the <url> child element. | No | agent |
| D019 | M001/S02/T02 | architecture | KillSession.SessionID without omitempty | SessionID uint32 with no omitempty | session-id is a required RFC 6241 field on kill-session. Using omitempty would suppress the element when SessionID=0, producing invalid XML. While session-id 0 is unusual, the RFC does not exclude it. Required fields must never have omitempty. | No | agent |
| D020 | M001/S02/T02 | architecture | EditConfig.Config as []byte innerxml | Config []byte with xml:",innerxml" | The config payload is arbitrary schema-specific XML that the library has no knowledge of. Using innerxml preserves it as raw bytes. Consistent with RPC.Body (S01) and RPCError.Info (S02/T01). | No | agent |
| D021 | M001/S03 | architecture | How the Client accesses the Session's transport for sending/receiving messages | Add narrow Send([]byte) error and Recv() ([]byte, error) methods to Session, delegating to transport.WriteMsg/ReadMsg on the internal trp field | Session.trp is unexported. Exporting it or adding a Transport() accessor would leak the transport type and break encapsulation. Send/Recv is the minimal interface the client needs — it preserves the single-goroutine contract (client serialises Send under a mutex, Recv in one dispatcher goroutine) and makes future transport changes transparent to the client. The research doc (S03-RESEARCH) recommends this exact approach. | Yes | agent |
| D022 | M001/S03 | architecture | Package placement for the Client library API | New package netconf/client with Client struct; sub-package of the root netconf package | Placing the Client in a sub-package (netconf/client) keeps the root netconf package clean — it contains only protocol types and session logic. The client package is the integration layer that composes Session + operations + error handling. This follows the same pattern as netconf/transport and netconf/transport/ssh. Research doc recommends this to avoid polluting the protocol types namespace. | Yes | agent |
| D023 | M001/S03 | architecture | Concurrent RPC dispatcher implementation strategy | Buffered channels (make(chan rpcResult, 1)) per pending RPC, with atomic.Uint64 for message-id generation starting at 1 | Buffered channel (size 1) ensures the dispatcher goroutine never blocks when sending a result, even if the caller has abandoned the wait due to context cancellation. atomic.Uint64 gives lock-free monotonically increasing message-ids without mutex contention. Decimal string format is conventional for NETCONF (RFC 6241 §4.1 says message-id is a string with no mandated format). Starting at 1 avoids the confusing message-id="0". | Yes | agent |
| D024 | M001/S03/T02 | architecture | checkReply / checkDataReply RPCError return shape | checkReply returns the first RPCError directly (RPCError implements error); callers use errors.As to extract structured fields | Avoids introducing a new named slice type for the common case where a single rpc-error is authoritative. errors.As is idiomatic Go and works through any error wrapping chain. If multiple errors matter (a future extension), callers can call ParseRPCErrors directly. | Yes | agent |
| D025 | M001/S03/T02 | architecture | Dial context propagation into SSH layer | ctx.Err() is checked before SSH dial but ctx is not threaded into gossh.Dial — SSH timeout uses config.Timeout | golang.org/x/crypto/ssh.Dial does not accept a context parameter. Threading ctx requires either a goroutine+cancel race or a library fork. The config.Timeout path is the SSH-standard mechanism. Future improvement when the library gains context support. | Yes | agent |
| D026 | M001/S04 | architecture | Server Handler interface signature and dispatch design | Handler.Handle(ctx, *Session, *RPC) ([]byte, error) — receives raw RPC, returns raw body bytes for rpc-reply (nil → ok), or RPCError for rpc-error. Server dispatches by extracting operation name from RPC.Body via xml.Decoder Local name. close-session is intercepted as a built-in before handler lookup. Single-threaded Serve loop — handler called synchronously, no goroutines inside dispatch. | Using raw *RPC avoids requiring the server to know all 13 operation types — handlers unmarshal the body themselves. The ([]byte, error) return covers all three reply states: ok, data, and error. close-session as a built-in prevents user handlers from interfering with session termination semantics. Single-threaded loop avoids concurrent Session.Send calls (Session is not goroutine-safe). Consistent with the echoServer prototype in client_test.go (P009). | Yes | agent |
| D027 | M001/S05/T01 | testing | Conformance test package placement and structure | External test package `conformance_test` in `netconf/conformance/`; loopback and SSH pair helpers scoped to the package; all assertions via testify | External test package provides real-user perspective (same import experience as library consumers). Separate `netconf/conformance` directory keeps conformance tests logically separate from per-package unit tests without requiring cross-package _test imports. Helper functions (`newLoopbackPair`, `newSSHPair`, `waitServe`) are package-scoped so conformance tests are self-contained. | Yes | agent |
| D028 | M001/S05/T01 | testing | newLoopbackPair returns srv un-served; callers register handlers before Serve | `newLoopbackPair` returns `*loopbackPair` with `.srv` not yet running; callers call `p.srv.RegisterHandler` before `p.startServe` | Handler registration and Serve start are sequential (same goroutine), eliminating a race between the first incoming RPC and handler lookup. If Serve started inside `newLoopbackPair`, any RPC dispatched before the caller's RegisterHandler calls would hit the "operation-not-supported" path spuriously. | No | agent |
| D029 | M001/S05/T02 | documentation | README Go version reflects go.mod actual value, not plan's stated minimum | README states "Go 1.26 or later" to match `go 1.26` in go.mod | Plan stated "Go 1.22 or later". The actual go.mod constraint is `go 1.26`. README must reflect the real requirement so users aren't surprised by build failures on 1.22–1.25. Always verify go.mod before documenting version requirements. | Yes | agent |
| D030 | M002 planning | architecture | Notification client API model | Full client + server notification stack in M002. Client gets notification channel/callback API; server gets notification emission. recvLoop branches on message type. | Complete notification stack is needed for practical use. Deferring client notification handling would leave the library unable to receive events, which is the whole point of RFC 5277. | Yes | collaborative |
| D031 | M002 planning | architecture | Interleave capability support | Support :interleave capability (RFC 5277 §3.2). Server processes RPCs while notification subscription is active. | Most real devices and clients expect concurrent RPCs alongside notifications. Without interleave, only close-session works during a subscription — too limited for practical use. | Yes | collaborative |
| D032 | M002 planning | architecture | TLS username derivation depth | Implement full RFC 7589 §7 cert-to-name mapping algorithm with fingerprint-based ordered mapping list and configurable map types. | NETCONF requires authenticated client identity. TLS transport must provide the same username abstraction as SSH. Full cert-to-name is the standard approach. | Yes | collaborative |
| D033 | M002 planning | architecture | With-defaults API integration approach | Integrate with-defaults as a field on existing Get, GetConfig, CopyConfig structs rather than standalone types. | The with-defaults parameter is specified as an augmentation to existing operations (RFC 6243 §4.5). Integrating it into existing types matches the RFC model and gives callers a natural API. | Yes | collaborative |
| D034 | M002 planning | architecture | Partial-lock XPath handling | XPath expressions in partial-lock are opaque strings. The library marshals them but does not evaluate them. Server implementers are responsible for XPath evaluation. | An XPath evaluator is a large, complex component outside the scope of a NETCONF protocol library. The library's job is wire format, not data model evaluation. | Yes | collaborative |
| D035 | M002 planning | architecture | Monitoring structs depth | Full Go struct coverage for all ietf-netconf-monitoring types (sessions, schemas, statistics, datastores). No built-in server handler — implementers populate structs and return from their own handlers. | Complete struct coverage gives implementers typed access to all monitoring data. A built-in handler would require the library to track session state, which is the server implementer's domain. | Yes | collaborative |
| D036 | M002/S01 | architecture | Notification channel capacity and back-pressure strategy | Buffered channel (capacity 64) with non-blocking send. Notifications are silently dropped when the channel is full, preventing dispatcher stall. The channel is created at Client construction time and closed when recvLoop exits. | A blocking send in recvLoop would stall the entire dispatcher (blocking RPC reply delivery too), which is unacceptable. A non-blocking send with drops is the pragmatic library choice — it protects the core RPC dispatch path while documenting that slow receivers may lose notifications. Capacity 64 is generous for typical NETCONF use cases. Callers who need guaranteed delivery can use a goroutine to drain the channel immediately. | Yes | agent |
| D037 | M002/S01 | architecture | Server notification emission API shape | SendNotification is a package-level function in the server package, not a method on Server. It takes a *Session and *Notification, marshals, and calls sess.Send. Callers must serialize concurrent access to sess externally. | Notification emission is orthogonal to RPC dispatch — a Server may not even be involved (e.g. a test or proxy). Making it a package-level function keeps the Server type focused on RPC dispatch. The concurrency documentation matches Session.Send's existing contract (not safe for concurrent use). Adding a session-level send mutex is deferred — if S05 integration tests reveal races, a mutex can be added then. | Yes | agent |
| D038 | M002/S02 | architecture | TLS transport package naming and stdlib aliasing | Package declared as `package tls` in `netconf/transport/tls/` directory, with internal alias `cryptotls "crypto/tls"` for the stdlib. Imported externally as `nctls "github.com/GabrielNunesIT/netconf/transport/tls"` in client.go. | Mirrors the SSH transport pattern exactly: `netconf/transport/ssh/` declares `package ssh` and aliases the external dependency as `gossh`. The TLS package follows the same convention — `package tls` matches the directory name, `cryptotls` avoids the stdlib naming collision internally, and `nctls` provides a clear alias for external consumers. | Yes | agent |
| D039 | M002/S02 | architecture | TLS ServerTransport peer cert exposure API | ServerTransport exposes `PeerCertificates() []*x509.Certificate` method. Callers (including the NETCONF server) call `DeriveUsername` themselves with their own cert-to-name mapping configuration. The transport does not perform username derivation. | Decouples username derivation from the transport layer (per D032). The mapping configuration is a server-level concern, not a transport concern. This matches the SSH transport's approach where authentication is handled by the SSH library and the transport just passes through the connection. | Yes | agent |
| D040 | M002/S03 | architecture | WithDefaults XML encoding strategy for the with-defaults parameter on Get/GetConfig/CopyConfig | Use a `WithDefaultsParam` struct with `XMLName` carrying the with-defaults namespace (`urn:ietf:params:xml:ns:yang:ietf-netconf-with-defaults`), embedded as `WithDefaults *WithDefaultsParam` with omitempty on the operation structs. The struct emits `<with-defaults xmlns="...">MODE</with-defaults>` as a child element. | RFC 6243 §4.5.1 specifies the with-defaults parameter as a child element in a separate namespace from the NETCONF base namespace. Go's encoding/xml requires the namespace to be on the struct's XMLName to emit the correct xmlns attribute. A dedicated struct (rather than a plain string field) is needed to carry the namespace. The pointer-with-omitempty pattern (P004) ensures nil produces no XML output, maintaining backward compatibility for existing callers. | Yes | agent |
| D041 | M002/S03/T02 | architecture | How client typed methods decode operation-specific reply bodies (PartialLockReply pattern) | Run checkReply first to surface rpc-error as *RPCError, then xml.Unmarshal(reply.Body, &typed) for the domain struct. Decode failures are wrapped with "client: METHOD: decode REPLY_TYPE: ..." prefix. | Two-step decode gives callers distinct error shapes for protocol-level errors (*RPCError, usable with errors.As) vs. decode failures (plain error with decode prefix). checkReply already handles the rpc-error/ok/data distinction; adding decode on top of it is minimal and follows the same layered error pattern as checkDataReply. The "decode" infix in the error message distinguishes schema mismatches from RPC errors in log lines without needing structured error types. | Yes | agent |
| D042 | M002/S04 | architecture | Monitoring partial-lock struct naming to avoid collision with operation type | `PartialLockInfo` in the `monitoring` package to distinguish from `netconf.PartialLock` (the operation type from S03) | The `netconf` package already has a `PartialLock` type (the RFC 5717 operation). The monitoring package's partial-lock struct represents lock state data in the `<netconf-state>` tree, not an operation. Using `PartialLockInfo` avoids confusion when both packages are imported. The `monitoring` package is a separate Go package so there's no true name collision, but the distinct name improves clarity in code that references both. | Yes | agent |
